# 01 - 基础开发环境与工具链搭建 (uv & Rust)

Type: task
Status: open
Blocked by:

## Question

如何在当前 Windows 11 环境下为本项目配置规范的 Python 3.13 + `uv` 包管理器以及稳定版 `rustup` + `cargo` 工具链，使得后续能够顺利执行 `uv sync`、`pytest`、`cargo check` 和 `cargo test`？
